---
description: PicAITool 项目上下文与约定，便于后续会话不丢失记忆
alwaysApply: true
---

# PicAITool 项目记忆

## 项目是什么
- 全品类组图营销站复刻（品牌名 PicAITool）。前端 React 18 + Vite + Tailwind，后端 Express，AI 为 Google Gemini。
- 核心流程：全品类组图 = 上传产品图 + 组图要求 → 分析（文+图→文）→ 确认规划 → 生图（Nano Banana/2/Pro）→ 完成。

## 关键文件
- **前端**：`src/pages/DetailSet.jsx`（步骤 1–5、分析/生成、整体设计规范与图片规划 Markdown 渲染、重置、步骤5 返回上一步/新建项目、自定义确认弹窗、生成图保存到本地/仓库）、`src/pages/ImageEdit.jsx`（**修改图片**，7种编辑模式，含 ModeDemo 可视化示例）、`src/pages/dashboard/Gallery.jsx`（仓库列表、按日期分组、多选、批量保存到本地、批量删除）、`src/components/layout/DashboardLayout.jsx`（侧栏仅「仓库」入口）、`src/components/layout/Header.jsx`（导航：工具区居中，订阅+用户区靠右）、`src/lib/clarityByModel.js`（模型与清晰度联动）、`src/context/AuthContext.jsx`（getToken 用于生成与仓库）。
- **后端**：`server/index.js`（注册/登录、分析、生图、**仓库 API**、**`/api/image-edit`（修改图片7种模式）**；生图请求带 token 时自动将本批图写入该用户仓库；生图 prompt 含 safeAreaRule 要求文字完整不裁切）、`server/db.js`（SQLite、gallery 表）、`server/gemini-models.js`（IMAGE_MODEL_IDS、分析模型、Nano 仅 1K 清晰度）。
- **配置**：`server/.env`（GEMINI_API_KEY、HTTPS_PROXY），从 `.env.example` 复制，不提交。
- **静态演示图**：`public/demo-*.png`（修改图片各模式 before/after 示例图，共 14 张）。

## 约定
- 分析模型默认 `gemini-2.5-flash`（`server/gemini-models.js` 中 `ANALYSIS_MODEL_ID`，可通过环境变量 `GEMINI_ANALYSIS_MODEL` 覆盖）；主备模型相同，503 重试机制保留但实际不切换。生图用 `getImageModelId(displayName)`。
- **组图要求输入字段**：已拆分为 5 个独立输入框——产品名称（必填）、卖点、目标人群、风格、其他要求。前端在 `runAnalyze` 中组装为结构化文本（`产品名称：...\n卖点：...\n`）后作为 `requirements` 发给后端，后端/AI prompt 不变。产品名称未填则禁止分析。
- 生成完成后：改「产品名称/卖点/目标人群/风格/其他/产品图/生成数量」→ 回到步骤 1 并清空分析/生成结果；改「目标语言、模型、尺寸、清晰度」→ 可直接再次生成。
- 生图 prompt 中已含产品自然摆放约束（避免凳子站在桌子上等）；**文字安全区**：safeAreaRule 要求整段标题完整在画面内、四周留足边距、禁止单词在边缘被裁切。
- **仓库**：侧栏只有「仓库」链接到 `/dashboard/gallery`；`/dashboard` 重定向到 `/dashboard/gallery`；仓库页支持多选、批量保存到本地、批量删除选中。
- **网络/稳定性**：分析/生图传图前前端用 `fileToCompressedDataUrl`（1024px、jpeg 0.82）压缩；分析接口遇 socket 关闭/ fetch failed 自动重试最多 3 次；生图在 prompt 中强调 aspect ratio（如 1:1 必须方图），后端打 aspectRatio 日志。1:1 输出是否稳定待用户次日验证。
- 回复用户时使用简体中文。

## 待办 / 已知问题（不丢失记忆）

- 设计规范与图片规划**已改为 Markdown 渲染**（`react-markdown` + `remark-breaks`），不再做自定义换行解析。仓库已用 **SQLite**（`server/db.js` + `picaitool.db`）+ `server/gallery/` 存图；登录用户生成图**自动写入仓库**；侧栏仅「仓库」、支持多选与**批量保存到本地、批量删除**；生图 prompt 已强化文字不裁切（safeAreaRule）。步骤 5 有「返回上一步」「新建项目」及自定义确认弹窗。
- **组图要求**已拆分为 5 个独立输入框（产品名称必填/卖点/目标人群/风格/其他），前端组装后发给后端。详细字段与 AI 传参说明见 `docs/DATA-FLOW.md`。
- **修改图片**（`/image-edit`）：7 种 Gemini 图片编辑模式——添加/移除元素、局部重绘、风格迁移、高级合成（多图）、高保真细节保留、让草图变生动、角色一致性360°。后端接口 `/api/image-edit` 含重试逻辑（Pro/Nano2 最多3次）。每种模式顶部有真实 before→after 可视化示例（图片存于 `public/demo-*.png`），`ModeDemo` 组件支持「单→单」「多→单」「单→多」三种布局。
- **导航栏重构**：删除「图片精修」入口；工具链接居中（万能画布/产品组图/风格复刻/服装组图/修改图片），「订阅」+用户区移至右侧。
- **仓库保存/下载**：单张用 showSaveFilePicker（另存为）正常；多张用 showDirectoryPicker（选一次文件夹），部分目录会被浏览器拒绝（「含有系统文件」），用户对该块体验不满意，**待后续再改**；约束见 `docs/KNOWN_ISSUES.md`（多张不能改为每张弹另存为，否则 1 万张要点 1 万次）。更多记录见 **`docs/KNOWN_ISSUES.md`**。
