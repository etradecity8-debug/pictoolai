---
description: PicToolAI 项目上下文与约定，便于后续会话不丢失记忆
alwaysApply: true
---

# PicToolAI 项目记忆

## 项目是什么
- 全品类组图营销站复刻（品牌名 PicToolAI）。前端 React 18 + Vite + Tailwind，后端 Express，AI 为 Google Gemini。
- 核心流程：全品类组图 = 上传产品图 + 组图要求 → 分析（文+图→文）→ 确认规划 → 生图（Nano Banana/2/Pro）→ 完成。

## 关键文件
- **前端**：`src/pages/DetailSet.jsx`（步骤 1–5、分析/生成、整体设计规范与图片规划 Markdown 渲染、重置、步骤5 返回上一步/新建项目、自定义确认弹窗、生成图保存到本地）、`src/pages/ImageEdit.jsx`（**修改图片**，9种编辑模式，含 ModeDemo 可视化示例）、`src/components/GalleryThumb.jsx`（**从作品库选择**缩略图：COS 绝对 URL 直接 img src，相对路径 Token fetch 转 blob，选图弹窗加载更快）、`src/components/OutputSettings.jsx`（**输出设置**：模型/比例/清晰度+积分预估，AI 美工各模块与官方示例一致）、`src/pages/AmazonAPlus.jsx`（**A+ 详情页生成**，4步流程，MVP）、`src/pages/AiAssistant.jsx`（**电商AI运营助手 → 亚马逊 → 生成 Listing**：Step 1 分析 → Step 2 标题/关键词/五点/描述 → Step 3 产品图 → Step 4 A+ 文案与图片；保存到 Listing 历史、导出 CSV）、`src/pages/dashboard/ListingHistory.jsx`（Listing 历史列表与详情）、`src/pages/StyleClone.jsx`（**风格复刻**，参考图+产品图→两阶段生图，入口在 AI美工 侧边栏）、`src/pages/Admin.jsx`（**管理后台**，客户列表/充值弹窗/流水弹窗/删除，路由 `/admin`，仅 admin 角色可访问）、`src/pages/dashboard/Gallery.jsx`（仓库列表、按日期分组、多选、批量保存/删除、**用AI编辑**：每张图「用AI编辑」→ 一二级菜单选功能 → 跳转 AI 美工并自动带图）、`src/lib/loadGalleryImage.js`（**从仓库 URL 加载图片**为 `{ file, dataUrl }`，供仓库→AI编辑带图及各页 initialImageFromGallery）、`src/components/layout/DashboardLayout.jsx`（侧栏含「仓库」「Listing 历史」）、`src/components/layout/Header.jsx`（导航：电商生图/AI美工/电商AI运营助手，风格复刻在 AI美工 下，隐藏 A+ 页面/服装组图/万能画布）、`src/pages/AiDesigner.jsx`（**AI美工**，路由 `/ai-designer`、`/ai-designer/:toolId`，侧边栏含图片编辑/质量提升/图像修复/抠图工具/**文字修改**（文字替换、语言转换、文字去除）/**风格变迁**（风格复刻、风格改变）/水印添加/去除/官方示例；接收 `location.state` 传 **initialImageFromGallery** 给各子页）、`src/pages/ai-designer/AddPersonObject.jsx`（**添加人/物**：人物最多3位+物品最多3件，自定义位置、真实比例，add-remove/composition）、`src/pages/ai-designer/LocalRedraw.jsx`（局部重绘）、`src/pages/ai-designer/LocalErase.jsx`（局部消除）、`src/pages/ai-designer/OneClickRecolor.jsx`（**一键换色**）、`src/pages/ai-designer/ProductRefinement.jsx`（**提升质感**，8 Tab 材质预设（电子/塑料/纺织/木质/玻璃/工业/自然/轻奢）+ 自定义输入）+ 保存按钮紧贴精修后图）、`src/pages/ai-designer/SmartExpansion.jsx`（**智能扩图**）、`src/pages/ai-designer/SceneGeneration.jsx`（**生成场景**，产品图+场景描述+风格→Lifestyle 图）、`src/lib/clarityByModel.js`（模型与清晰度联动）、`src/lib/pointsEstimate.js`（积分预估）、`src/lib/exportListingCsv.js`（导出亚马逊/eBay/速卖通 Listing CSV/JSON）、`src/context/AuthContext.jsx`（getToken 用于生成与仓库；`isAdmin` 便捷属性判断角色）。
- **后端**：`server/index.js`（注册/登录、**注册时 grantSignupBonus 送 150 积分**、分析、生图、**仓库 API**、`/api/image-edit`（修改图片，含 inpainting/add-remove/**recolor** 等模式）；**AI 助手亚马逊**：`/api/ai-assistant/amazon/*`；`/api/amazon-aplus/*`；**风格复刻**：`/api/style-clone`；**管理员 API**（`/api/admin/users` 列表/充值/删除/流水/改角色）；启动时自动迁移 users.json → SQLite、自动提升 ADMIN_EMAIL 为管理员）、`server/db.js`（SQLite：users/gallery/user_points/points_transactions/amazon_listing_snapshots 等表）、`server/points.js`（积分规则、getBalance 含惰性清零、grantPoints/grantSignupBonus、getSubscriptionInfo）、`server/gemini-models.js`（IMAGE_MODEL_IDS、分析模型、Nano 仅 1K 清晰度）。
- **配置**：`server/.env`（GEMINI_API_KEY、HTTPS_PROXY、**ADMIN_EMAIL、ADMIN_PASSWORD**、**COS_SECRET_ID/COS_SECRET_KEY/COS_BUCKET/COS_REGION**（可选，COS 加速）），从 `.env.example` 复制，不提交。
- **静态演示图**：`public/demo-*.png`（修改图片各模式 before/after 示例图，共 14 张）；`public/local-redraw-demo-*.png`、`public/local-erase-demo-*.png`、`public/recolor-demo-*.png`（AI 美工局部重绘/局部消除/一键换色）；`public/demo-smart-expansion-before.png`、`public/demo-smart-expansion-after.png`（智能扩图）；`public/demo-add-person-object-before.png`、`public/demo-add-person-object-after.png`（添加人/物示例）。

## 文档结构（2026-03 已合并，共 8 个）

- **docs/README.md**：总索引，含「快速找文档」表。
- **PROJECT-OVERVIEW.md**：项目结构、技术栈、功能一览。
- **ECOMMERCE-GENERAL-CREATE-PICTURES.md**：电商生图（全品类组图）字段与 AI 传参。
- **ECOMMERCE-AI-ASSISTANT.md**：电商 AI 运营助手（亚马逊 Listing + 生图提示词 + A+ 模块）。
- **AI-DESIGNER.md**：AI 美工（局部重绘、局部消除、一键换色、智能扩图、提升质感、官方示例 9 种）。
- **DEPLOY.md**：VPS 部署步骤，含附录「Gemini 地区限制」。
- **COS-CDN.md**：仓库图片国内加速（腾讯云 COS + 临时签名 URL）；实现说明、全代码影响、最小测试清单。
- **KNOWN_ISSUES.md**：已知问题与待办。
- **打印版**：`docs/print/`，`npm run docs:print` 生成；`index.html` 手工维护链接。

## 文档约定（重要，不丢失）

- **根目录**：只有 **README.md** 一个文档，仅做入口（项目一句话介绍、快速开始、指向 docs 的链接表）。不在此写长说明、不重复 docs 里的内容。
- **所有详细文档都在 `docs/`**：产品设计、运维部署、已知问题、项目概览等，一律写在 `docs/*.md`。**新增或大改文档时，只往 docs 里放，不要在外面新建散落的 .md。**
- **`docs/README.md` = 文档总索引**：新增或重命名文档后，**必须**在 `docs/README.md` 里对应分类下加一行（表格或列表），写清文件名和一句话说明，避免文档「找不到」。
- **打印版**：`docs/print/` 下为可打印 HTML。生成方式：项目根目录执行 `npm run docs:print`（会遍历 `docs/*.md` 生成 HTML）。若在 `docs/print/index.html` 里做了手工链接，新增文档后记得在索引页补一条。
- **写文档时**：新功能/新设计/运维说明 → 在 `docs/` 下新建或更新对应 .md → 更新 `docs/README.md` 索引。不确定放哪时，优先放在「项目与功能」或「已知问题」类，或新建一个语义清晰的文件名（如 `XXX-SPEC.md`、`XXX-GUIDE.md`）。

## 约定
- 分析模型默认 `gemini-2.5-flash`（`server/gemini-models.js` 中 `ANALYSIS_MODEL_ID`，可通过环境变量 `GEMINI_ANALYSIS_MODEL` 覆盖）；主备模型相同，503 重试机制保留但实际不切换。生图用 `getImageModelId(displayName)`。
- **组图要求输入字段**：已拆分为 5 个独立输入框——产品名称（必填）、卖点、目标人群、风格、其他要求。前端在 `runAnalyze` 中组装为结构化文本（`产品名称：...\n卖点：...\n`）后作为 `requirements` 发给后端，后端/AI prompt 不变。产品名称未填则禁止分析。
- 生成完成后：改「产品名称/卖点/目标人群/风格/其他/产品图/生成数量」→ 回到步骤 1 并清空分析/生成结果；改「目标语言、模型、尺寸、清晰度」→ 可直接再次生成。
- 生图 prompt 中已含产品自然摆放约束（避免凳子站在桌子上等）；**文字安全区**：safeAreaRule 要求整段标题完整在画面内、四周留足边距、禁止单词在边缘被裁切。
- **仓库**：侧栏只有「仓库」链接到 `/dashboard/gallery`；`/dashboard` 重定向到 `/dashboard/gallery`；仓库页支持多选、批量保存到本地、批量删除选中。**用AI编辑**：每张图有「用AI编辑」按钮，点击后弹窗**一二级菜单**（与 AI 美工侧栏一致：图片编辑、质量提升、文字修改（3 项）、风格变迁、水印添加/去除、官方示例（7 项））选功能，跳转 `/ai-designer/:toolId` 并通过 `state` 传入图片信息，各 AI 美工页通过 `initialImageFromGallery` + `loadImageFromGalleryUrl` 自动加载该图。**业务逻辑（全站统一）**：只要生图成功，图片都**自动进入仓库**；生图完成后图片旁仅「保存到本地」，不提供单独的「保存到仓库」按钮。**COS 加速（可选）**：配置 `COS_*` 环境变量后，新图自动上传腾讯云 COS，列表返回临时签名 URL（1 小时有效）；未配置时仅用本地文件，行为不变。Gallery 页对 COS 绝对 URL 直接 img src 展示（不 fetch，避免 CORS）；相对路径用 Token fetch 转 blob 展示；下载/批量下载统一走 `/api/gallery/image/:id`（带 Token，从本地文件读取）。拉图失败时显示「加载失败」+「点击重试」。详见 `docs/COS-CDN.md`。
- **网络/稳定性**：分析/生图传图前前端用 `fileToCompressedDataUrl`（1024px、jpeg 0.82）压缩；分析接口遇 socket 关闭/ fetch failed 自动重试最多 3 次；生图在 prompt 中强调 aspect ratio（如 1:1 必须方图），后端打 aspectRatio 日志。1:1 输出是否稳定待用户次日验证。
- 回复用户时使用简体中文。
- **协作约定**：客户对 Git、GitHub、服务器部署不熟悉。进行提交/推送、部署、更新代码等操作时，要**一步一步**给出命令与说明，避免一次列很多步骤或假设对方已会基本操作。

## 待办 / 已知问题（不丢失记忆）

- 设计规范与图片规划**已改为 Markdown 渲染**（`react-markdown` + `remark-breaks`），不再做自定义换行解析。仓库已用 **SQLite**（`server/db.js` + `pictoolai.db`）+ `server/gallery/` 存图；登录用户生成图**自动写入仓库**；侧栏仅「仓库」、支持多选与**批量保存到本地、批量删除**；生图 prompt 已强化文字不裁切（safeAreaRule）。步骤 5 有「返回上一步」「新建项目」及自定义确认弹窗。
- **组图要求**已拆分为 5 个独立输入框（产品名称必填/卖点/目标人群/风格/其他），前端组装后发给后端。详细字段与 AI 传参说明见 `docs/ECOMMERCE-GENERAL-CREATE-PICTURES.md`。
- **修改图片**（`/image-edit`）：现有 **9 种**模式——添加/移除元素、局部重绘、风格迁移、高级合成（多图）、高保真细节保留、让草图变生动、角色一致性360°、文字替换、语言转换。后端接口 `/api/image-edit` 含重试逻辑（Pro/Nano2 最多3次）。每种模式顶部有真实 before→after 可视化示例（`ModeDemo` 组件）。修改后图片自动入图库，显示积分消耗，支持从仓库选图（`GalleryThumb` 组件 fetch+blob URL 认证加载）。
- **亚马逊 A+ 详情页**（`/amazon-aplus`）：**MVP 阶段**，4步流程：填写信息→确认文案→生成图片→查看结果。两个后端接口：`/api/amazon-aplus/analyze`（文案，不扣积分）+ `/api/amazon-aplus/generate`（顺序生成4张图，扣积分，存图库）。支持10种语言、3种风格。图文一一对应，所有文案一键复制。**完整方案待后续迭代**（A/B对比模块、规格图、导出ZIP等），详见 `docs/KNOWN_ISSUES.md`。
- **A+ 图片生成注意**：4张图顺序生成（非并行），避免 Nano Banana 2 并发限速；prompt 中加最高优先级禁文字规则，防止 AI 将产品名渲染到图上（尤其中文产品名）；积分按实际成功张数扣除；图库标题与实际生成图绑定（不因失败导致标题错位）。
- **导航栏**：当前显示「电商生图」「AI美工」「电商AI运营助手」；右侧含「订阅」「联系我们」链接；**风格复刻** 已移入 AI美工 侧边栏；**已隐藏**：「A+ 页面」「服装组图」「万能画布」。
- **电商AI运营助手**：Header 无下拉，点击直接进 `/ai-assistant`，平台在 AiAssistant 页左侧侧边栏选。**亚马逊**功能卡片：生成 Listing、优化 Listing、竞品对比、关键词研究；**eBay** 和 **速卖通** 功能卡片：生成 Listing、优化 Listing。**侵权风险检测**：侧栏单独一项「侵权风险检测」（`?platform=ip-risk`），多图+选填说明。免费快筛（Gemini，返回 `sections` 时分组卡片）；深度查询（SerpApi：Google Lens 以图搜图+Google Patents 专利+Google 搜索商标，10 积分，也返回 `sections` 或 `report`）。深度含 **searchSummary**（method/service/content/purpose），前端表「检索方式（使用服务）」；用途说明以图=整网、专利=主要美国、商标=以美国为主。报告卡片从正文解析风险等级展示在标题行，「高」红色+警告图标。`POST /api/ai-assistant/ip-risk-check`，深度需 `SERPAPI_KEY`。客户沟通与费用见 `docs/IP-RISK-SERVICES-AND-COST.md`。
- **亚马逊 Listing 工具链**：
  - **优化 Listing**：诊断+一键优化（A9/Cosmo/Rufus/GEO 四维度），不扣积分。双语诊断报告（原文/中文切换）；合规自检 9 大类（error/warning/info 三级）。
  - **A/B 文案变体**：优化/生成 Listing 结果区可点「生成 A/B 变体」，后端 `/api/ai-assistant/amazon/generate-variants`，生成 2-3 套不同策略角度文案，Tab 切换对比，不扣积分。
  - **竞品对比**：`/api/ai-assistant/amazon/competitor-compare`，输入自己+1-5 个竞品 Listing（Tab 切换输入，可增删），输出关键词差异、卖点矩阵、策略分析、行动计划，不扣积分。
  - **关键词研究**：`/api/ai-assistant/amazon/keyword-research`，输入产品名称，输出核心词、长尾词分组、后台关键词建议、标题排布方案，不扣积分。
- **eBay/速卖通 Listing 工具链**：
  - **生成 Listing**：analyze 接口已升级为深度产品智能（提取 topKeywords/buyerQuestions/buyerPersonas），generate-listing 为策略驱动 prompt，前端展示产品洞察。
  - **优化 Listing**：`/api/ai-assistant/ebay/optimize-listing`、`/api/ai-assistant/aliexpress/optimize-listing`，Cassini/速卖通搜索算法诊断，合规自检（三级），双语诊断报告，`PlatformOptimizeForm` 组件 eBay/速卖通共用。
- **智能粘贴**：`/api/ai-assistant/smart-paste`，用户可全选复制 Listing 页面内容一次性粘贴，AI 自动提取标题/五点/属性/描述等字段填充到表单。三处前端入口：亚马逊优化 Listing、eBay/速卖通优化 Listing、竞品对比。
- **联系我们**：`/contact` 仅微信，有「返回主页」；定价页选套餐弹 ContactModal；Header 有入口。
- **主页**：Hero 黑色块居中、FeatureCards/FAQ 左对齐、CTA 黑色块；全用 `max-w-7xl` 与 Header 对齐；产品页与 ProductShowcase 已删；Footer「关于我们」「帮助中心」灰掉。
- **仓库保存/下载**：单张用 showSaveFilePicker（另存为）正常；多张用 showDirectoryPicker（选一次文件夹），部分目录会被浏览器拒绝（「含有系统文件」），**待后续再改**。约束见 `docs/KNOWN_ISSUES.md`。
- **客户积分管理**：已实现手动运营版。`users` 表替代 users.json；`user_points` 含 `expires_at`（到期惰性清零）；管理后台 `/admin`（仅 admin）支持充值/流水/删除。**新用户注册默认赠送 150 积分**（30 天有效期，`grantSignupBonus`）；注册页有「新注册用户赠送 150 积分供您体验」提示。**删除用户**：`DELETE /api/admin/users/:email` 会同时删除该用户的 `users`、`user_points`、`points_transactions`、`gallery`（及本地/COS 文件）、`amazon_listing_snapshots`、`ebay_listing_snapshots`、`aliexpress_listing_snapshots`；**删除后该邮箱可重新注册**（注册只查 users 表）。**管理员客户备注**：`users` 表有 `admin_notes` 字段（启动时自动迁移）；管理后台客户列表有「备注」列与「编辑备注」按钮，`PATCH /api/admin/users/:email/notes` 请求体 `{ adminNotes: "..." }`。`server/.env` 配置 `ADMIN_EMAIL`+`ADMIN_PASSWORD` 启动时自动提升。**安全**：已移除旧的 `/api/points/credit` 自助充值端点和前端按钮，积分充值仅限管理员 `/api/admin/users/:email/grant`。**积分预估**：`src/lib/pointsEstimate.js` 的 `getEstimatedPointsForDimensions` 已模拟前端压缩（compressMaxSize=1024），与后端实际扣费一致。待后续：支付接入、到期提醒邮件、前端套餐页、注册审批。详见 `docs/KNOWN_ISSUES.md`。
- **部署**：美国硅谷已部署完成（腾讯云轻量，43.162.87.60，域名 pictoolai.studio，Nginx 反代 + **HTTPS**）；**HTTPS** 已启用（Let's Encrypt/certbot，免费且自动续期）；腾讯云轻量**防火墙须放行 TCP 443**；PM2 需 `--cwd /home/ubuntu/app/server` 以正确加载 .env；香港已暂停（Gemini 不可用）。SSH 空闲断线常见，重连不影响 Nginx/PM2 等服务。详见 `docs/DEPLOY.md`、`docs/KNOWN_ISSUES.md`。
- **品牌与数据**：品牌名统一 **PicToolAI**，技术标识 **pictoolai**（`pictoolai.db`、`pictoolai_token`/`pictoolai_user`、PM2 示例 `pictoolai-server`）。旧 picaitool.db 与旧 gallery 已清理，仅用新库与新 gallery 目录。
- **亚马逊 Listing 生成与合规**：电商AI运营助手 → 亚马逊 → 生成 Listing（`/ai-assistant` 选亚马逊）。**流程**：Step 1 仅做分析（按钮「分析产品」）→ 展示分析结果，用户**确认分析结果**后才可进行 Step 2；不认可则「重新分析」返回输入界面；分析结果页只显示分析结果与按钮，不显示表单。Step 2 标题/后台关键词/五点/描述（不扣积分）整体放在与步骤 3、4 同款白底框内；**保存到 Listing / 导出 CSV** 放在所有步骤结束后单独一块。Step 3 产品图（主图白底，扣积分）、Step 4 A+（复用 amazon-aplus 接口）。**合规**：prompt 与后端已按 `docs/ECOMMERCE-AI-ASSISTANT.md` 约束；generate-listing prompt 已增补五类违禁与替代（促销/IP/医疗/杀虫剂与生物杀灭/环保等）；标题返回后去除 `! $ ? _ { } ^ ¬ ¦` 并截断。
- **生成 Listing Step 4 A+**：**当前已锁定**（`APLUS_STEP_ENABLED = false`），界面显示「功能优化中，敬请期待」。恢复方式：`AiAssistant.jsx` 中改 `APLUS_STEP_ENABLED = true`。原逻辑：用户点「生成 A+ 文案和图片」，背后先调 A+ 文案接口、再调 A+ 图片接口；步骤 3 与 4 可并行。
- **生成 Listing prompt 优化**：analyze 要求提取 Rufus 可用的具体参数（尺寸、重量、材质、认证等）写入 productSummary/keyAttributes；generate-listing 要求标题前 50–80 字符含核心词与属性、五点每条对应一个利益或买家问题并可含痛点→方案/场景/硬数据，并含五类违禁与替代说明。
- **Gemini 智能体/指令参考**：用户自有若干 Gemini 搭建 Listing 智能体相关文档（含工作流、指令模板、合规与违禁词等）；用户已删除此前转成的 txt，计划自行阅读后截取有用部分再提供给 AI 做后续优化。
- **保存 Listing**：所有步骤结束后有「保存到我的 Listing」「导出 CSV」「导出 JSON」；导出统一实现在 `src/lib/exportListingCsv.js`（支持亚马逊/eBay/速卖通）。保存时传 `mainImageId`/`aplusImageIds`，Listing 历史详情可展示关联主图与 A+ 图。Logo 已接入（`public/logo.png`），Header 内不溢出、与导航栏同底。
- **输出纯文本**：后端 `stripMarkdown` 函数（`server/index.js`）自动清除 AI 返回的 `**粗体**`、`_斜体_` 等 Markdown 格式，所有平台的 generate-listing / optimize-listing / generate-variants 输出的五点和描述均为纯文本，可直接粘贴到平台后台。
- **AI 美工**（`/ai-designer`）：侧边栏含图片编辑（局部重绘、局部消除、一键换色、**服装3D**、**服装平铺**、**调整身材**、**生成场景**、**添加人/物**）、质量提升（智能扩图、提升质感）、**文字修改**（文字替换、语言转换、**文字去除**）、**风格变迁**（风格复刻、风格改变）、水印添加/去除、官方示例（7 种：添加/移除元素、局部重绘(语义)、风格迁移、高级合成、高保真细节保留、让草图变生动、角色一致性360°）。**输出设置统一**：局部重绘、局部消除、一键换色、智能扩图、提升质感、服装3D、服装平铺、调整身材、生成场景、**添加人/物**均提供与官方示例一致的「输出设置」；**从作品库选择**用 `GalleryThumb`；**从仓库带图进入**：AiDesigner 从 `location.state` 取 `fromGallery`/`imageUrl` 等，以 `initialImageFromGallery` 传给各子页，各页用 `loadImageFromGalleryUrl` 拉图并设为初始图。**添加人/物**（`/ai-designer/add-person-object`）：人物最多 3 位、物品最多 3 件（文字或上传图），预设+自定义位置，prompt 强调真实比例；占位图 `demo-add-person-object-before/after.png`。**风格复刻**：路由 `/ai-designer/style-clone`。**文字修改**：含文字替换、语言转换、文字去除；文字替换示例图 `demo-text-replace-before/after.png`。**局部重绘**：遮罩+prompt，`mode: 'inpainting'`；**局部消除**：遮罩消除，`mode: 'add-remove'`；**一键换色**：`mode: 'recolor'`；**服装3D**/**服装平铺**/**调整身材**/**生成场景**/**智能扩图**/**提升质感**：见 `docs/AI-DESIGNER.md`。**Turbo 已移除**。详见 `docs/AI-DESIGNER.md`。
