# 已知问题与待办（后续再改）

## 1. 整体设计规范：主色调/辅助色/背景色换行未彻底解决

**现象**：右侧「整体设计规范」中，「色彩系统」下的「主色调」「辅助色」「背景色」仍经常连在同一行或同一段显示（堆在一起），未做到各自单独一行。

**相关代码**：
- 前端：`src/pages/DetailSet.jsx` 中的 `SpecPreview`、`splitBySecondaryLabels`、`parseLabelLine`。
- 当前已做：按 `##` 一级标题空行、按「 - 」与「、」「；」及标签前位置拆分、二级标签加粗与小号字。
- 未解决：部分分析结果（或 API 返回的 Markdown）格式与当前正则不匹配，导致未拆成多行。

**后续可尝试**：
1. **分析阶段**：在 `server/index.js` 的分析 prompt 中明确要求「主色调」「辅助色」「背景色」必须各占一段或每项后换行（例如：每项单独一行，用换行符分隔）。
2. **前端**：根据实际返回样本再补充分隔符（如更多标点、全角/半角）或放宽 `SECONDARY_LABELS` 的匹配规则；或对整段做「标签：」的全局匹配再按匹配位置切分。

**记录时间**：2026-03-05（用户反馈「你依旧没有能修改，请记录这个问题，我们后续再改」）。

---

## 文档与代码归档说明

- 项目说明与结构见根目录 `README.md`。
- 后端接口与模型配置见 `server/index.js`、`server/gemini-models.js`。
- 全品类组图流程、设计规范与图片规划逻辑见 `src/pages/DetailSet.jsx` 及本目录下的记录。
