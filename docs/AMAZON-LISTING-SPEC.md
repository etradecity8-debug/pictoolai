# 亚马逊 Listing 生成功能 — 产品设计方案

**版本**：v0.2 已确认  
**日期**：2026-03-08  
**状态**：已确认，开发中

**合规承诺**：本模块的生成/优化功能严格遵守亚马逊平台规则，并智能符合 A9（传统搜索）、Cosmo（语义搜索）、Rufus（AI 购物助手）、GEO（AI 搜索引擎优化）等原则。

---

## 一、功能概述

在「电商AI运营助手 → 亚马逊」模块中新增「生成 Listing」功能。  
用户填写产品信息后，AI 分多步自动生成符合亚马逊规则的完整 Listing，包括标题、后台关键词、五点描述、产品描述，并可进一步生成 A+ 页面文案和产品图。

---

## 二、输入设计

### 必填项（缺少任一项不允许提交）

| 序号 | 字段 | 说明 |
|------|------|------|
| 1 | 产品图片（至少 1 张） | 随手拍、供应商图均可；AI 从图中提取产品信息，上传数量建议 1–5 张 |
| 2 | 产品类别（二级联动下拉） | 一级约 15 个大类，二级联动子类；决定 Listing 格式与优化方向 |
| 3 | 品牌名 | 亚马逊店铺品牌名；无品牌填 Generic |
| 4 | 核心卖点（至少 2 条） | 用户最了解自己产品的差异化点；每条一行填写 |
| 5 | 目标市场 | 美国、英国、德国、法国、日本、加拿大、澳大利亚、墨西哥等 |

### 可选项（填写后 AI 输出质量更高）

| 字段 | 说明 |
|------|------|
| 货源链接 | 1688 / 速卖通 / Alibaba 等供应商页面链接；后端自动读取页面内容并补充产品信息 |
| 参考关键词 | 用户自己做过关键词研究的可以填入，AI 会优先覆盖这些词 |
| 特殊认证 / 备注 | 如 CE、FCC、BPA-free、专利、特别要强调的功能点等 |

---

## 三、产品类别设计（二级联动下拉）

**设计决策**：不抓取亚马逊完整类目树。原因：类目层级过深（叶子节点达数千个）、维护成本高，且对 Listing 文案生成而言，一二级大类已足够指导 AI 的写作风格和关键词策略。精确的 Browse Node 是卖家在 Seller Central 上架时才需要填写的，不属于文案范畴。

### 一级 → 二级示例

| 一级 | 二级示例 |
|------|---------|
| 家居厨房 | 厨具、收纳整理、家纺、灯具、清洁用品、浴室用品 |
| 运动户外 | 户外装备、健身器材、自行车、游泳用品、球类运动 |
| 电子数码 | 手机配件、电脑配件、音频设备、摄影器材、智能家居 |
| 美容个护 | 护肤、彩妆、发型护理、香水、健康护理 |
| 宠物用品 | 犬用、猫用、小动物、水族 |
| 婴儿用品 | 喂养、婴儿衣物、婴儿玩具、婴儿安全 |
| 服装鞋包 | 男装、女装、鞋类、箱包配件 |
| 玩具游戏 | 益智玩具、遥控玩具、户外玩具、桌游 |
| 汽车用品 | 内饰配件、外饰改装、工具、电子设备 |
| 工具五金 | 电动工具、手工具、建材五金 |
| 园艺 | 园林工具、种植用品、户外家具 |
| 健康医疗 | 维生素/保健品、医疗器具、急救用品 |
| 办公文具 | 文具耗材、打印耗材、办公家具 |
| 其他 | 用户手动补充描述 |

---

## 四、输出设计

### 4.1 输出内容与字符限制

| 序号 | 输出项 | 亚马逊字符限制 | 备注 |
|------|--------|----------------|------|
| 1 | 标题（Title） | ≤200 字符 | 含品牌名、核心搜索词、主要卖点 |
| 2 | 后台搜索关键词（Search Terms） | ≤250 bytes | 不与标题中已有的词重复 |
| 3 | 五点描述（Bullet Points） | 每条 ≤500 字符，共 5 条 | 按 Rufus 问答逻辑组织 |
| 4 | 产品描述（Product Description） | ≤2000 字符 | 适合尚未注册品牌的卖家 |
| 5 | A+ 文案（Enhanced Brand Content） | 多模块，约 3–5 个 | 本模块内新建 A+ 子流程，原有 A+ 页面暂不用 |
| 6 | 产品图 | 1～9 张（客户可选） | 第 1 张为主图（白底），第 2～9 张为附加图（多角度/场景）；本模块内生成，符合亚马逊主图/附图规则 |

### 4.2 四大优化维度

生成的所有文案需同时满足以下维度：

| 维度 | 说明 | 写法要点 |
|------|------|---------|
| **A9**（传统搜索算法） | 关键词相关性、点击率、转化率 | 标题前 80 字符含高搜索量核心词；五点自然分布长尾词；后台关键词不重复标题 |
| **Cosmo**（语义搜索，2023+） | 理解语义和使用场景，不只做关键词匹配 | 每个 bullet 用自然语言描述：谁在用、什么场景、解决什么问题 |
| **Rufus**（AI 购物助手，2024+） | 直接读取 Listing 回答买家问题 | 每个 bullet 隐含回答一个买家常见问题（如：会不会漏水？用多久？） |
| **GEO**（AI 搜索引擎优化） | 内容结构化、事实性强，可被 AI 引用 | 包含精确数据：尺寸、重量、材质、认证、测试结论等 |

---

## 五、AI 调用流程（多步生成）

分步调用 AI，前端按步骤逐步展示进度。每步独立，某步失败不影响其他步骤，用户可以单步重试或单步重新生成。

| 步骤 | 内容 | 输入 | 预估输出量 |
|------|------|------|-----------|
| Step 1 | **产品分析** | 图片 + 货源链接（可选） | 结构化 JSON：品类、材质、特点、潜在关键词 |
| Step 2 | **标题 + 后台关键词 + 五点描述** | Step1 结果 + 用户输入 | 约 1–1.5K tokens |
| Step 3 | **产品描述** | Step1 结果 + Step2 结果 | 约 1K tokens |
| Step 4 | **A+ 文案**（可选） | 以上全部结果 | 约 2–3K tokens，多模块 |
| Step 5 | **产品图**（可选） | 跳转至「电商生图」模块 | 走图像生成流程 |

---

## 六、已确认结论（2026-03-08）

| # | 问题 | 结论 |
|---|------|------|
| 1 | 货源链接 | 后续做，不纳入第一期 |
| 2 | A+ 文案 | 本模块内重新做新模块，原有 A+ 页面暂不用 |
| 3 | 产品图 | 本模块内生成，不跳转电商生图 |
| 4 | 输出语言 | 由用户单独选择，不与目标市场绑定 |
| 5 | 积分消耗 | 方案 C：仅生图步骤扣积分，文字生成免费（引流） |

---

## 七、亚马逊规则与合规约束

生成与优化 Listing 时，除 A9/Cosmo/Rufus/GEO 外，必须符合亚马逊以下硬性规则，并在 prompt 中显式约束。

### 7.1 标题（Title）

- **长度**：多数类目 ≤200 字符（2025 年 1 月起强制）；部分类目更短（如 80/50），需按类目校验。
- **禁止**：全大写、堆砌无关关键词、竞品/他人品牌名；**同一单词不得重复超过 2 次**（冠词/介词/连词除外）；**禁止使用** `! $ ? _ { } ^ ¬ ¦` 等符号（除非为品牌名一部分）。
- **建议结构**：`品牌 + 核心产品词 + 关键属性（材质/尺寸/数量等）+ 主要卖点`；**前 80 字符含核心搜索词**（亚马逊推荐，利于点击与搜索）。

### 7.2 五点描述（Bullet Points）

- **长度**：每条 ≤500 字符（Brand Registry/部分类目）；部分类目或账号为 255 字符，建议每条尽量控制在 255 以内以兼容；共 5 条，须全部使用且内容不重复。
- **禁止**：价格、促销、站外链接；夸大/绝对化/医疗未证宣称；**特殊符号**（™ ® €）与 **emoji**；**跨条重复**；ASIN、「N/A」「TBD」「not applicable」；公司信息、联系方式、网址；退款/质保承诺用语；类目禁用营销用语（如 eco-friendly、anti-bacterial、bamboo、soy 等，除非有认证）。
- **建议**：每条对应一个卖点或一个买家常见问题；信息具体、可验证（尺寸、材质、认证）；以利益为导向、简洁可读（移动端前 3 条与截断约 70–80 字符）。

### 7.3 产品描述（Product Description）

- **长度**：通常 ≤2000 字符（部分类目不同）。
- **禁止**：与五点逐句重复堆砌、站外链接、联系方式、违禁词（医疗/疗效/绝对化等）；**不得出现竞品对比、「best seller」「top-rated」等宣称**。
- **建议**：补充使用场景、规格、保养、包装内容等，与五点形成互补。

### 7.4 后台关键词（Search Terms）

- 总长 ≤250 bytes（多字节语言如中文、日文占更多 bytes）。
- 禁止：重复标题/五点中已有的词、竞品品牌、无关词堆砌。
- 建议：只填标题和五点中未出现的长尾词、同义词、拼写变体。

### 7.5 图片（参考亚马逊官方与 2024–2026 卖家指南）

- **主图**：纯白底 **RGB(255,255,255)**（不可偏灰/米白，否则可能被系统拒绝）；产品占画面 **≥85%**、完整可见、居中；**无文字/logo/水印/边框/多角度拼图**；**长边 ≥1000px** 方可启用缩放（建议 1600px 以上）；比例建议 1:1；格式 JPEG/PNG/TIFF/GIF，单文件 ≤10MB；仅展示实际售卖商品，无未含在售内的道具或包装。
- **附加图**：不违反类目与内容政策，不侵犯他人品牌/IP；可用于场景图、信息图等。
- **注意**：亚马逊政策要求主图为「实际拍摄」照片；AI 生成图在严格解读下可能不符合主图要求，建议用户将本工具生成图用作参考或附加图，或根据类目与站点政策自行判断。

### 7.6 通用合规

- 禁止：仿品、侵权（商标/版权/专利）、虚假陈述、成人/违禁内容。
- 谨慎：医疗/美容/食品等类目有额外合规要求（认证、禁用词表），生成与优化时需考虑类目限制。

### 7.7 A+ 内容（Enhanced Brand Content）

- **禁止**：提及竞品名称；使用「best seller」「top-rated」「Amazon's Choice」；保证/质保类表述；外部链接、社媒、二维码；联系方式或配送细节；医疗或未经支持的宣称；文案中出现特殊符号（™ ® €）或 emoji。
- **要求**：以利益为导向、版面清晰、品牌一致；图片符合亚马逊分辨率与版权要求；所有文案与图片需拥有使用权。

---

## 八、与现有功能的关系

| 现有功能 | 与本功能关系 |
|---------|------------|
| A+ 页面（`/amazon-aplus`） | 暂不用；本模块内新建 A+ 子流程 |
| 电商生图（`/detail-set`） | 不跳转；本模块内生成产品图 |
| 电商AI运营助手框架（`/ai-assistant`） | 本功能是「亚马逊」平台下的第一个子功能 |

---

## 九、实现细节（开发记录）

### 9.1 前端

- **文件**：`src/pages/AiAssistant.jsx`。
- **生成 Listing 表单**：
  - 必填：产品图片（≥1 张，最多 5 张，压缩为 JPEG data URL 后上传）、一级类目 + 二级类目（联动下拉）、品牌名、核心卖点（textarea 按行解析，≥2 条）、目标市场、输出语言。
  - 可选：参考关键词、特殊认证/备注。
  - 校验：未登录提示「请先登录」；缺必填项时按钮禁用并提示。
- **产品类别**：常量 `CATEGORY_TREE`，一级 14 类（家居厨房、运动户外、电子数码、美容个护、宠物用品、婴儿用品、服装鞋包、玩具游戏、汽车用品、工具五金、园艺、健康医疗、办公文具、其他），二级为各类子项数组，与本文档第三节一致。
- **流程**：提交 → Step 1 分析 → Step 2 标题·关键词·五点·描述 → 展示结果；结果页内可继续 Step 3 产品图、Step 4 A+ 文案与图片。步骤条为「1 分析 / 2 标题·关键词·五点·描述 / 3 产品图 / 4 A+」，支持 done/active 状态。
- **结果展示**：标题、后台关键词、五点、产品描述（可复制）；Step 3 区块可选「生成数量」1～9 张 → 主图 + 附加图展示并入库；Step 4 区块「生成 A+ 文案」→ 展示 copy，「生成 A+ 图片」→ 展示 Hero + 特点图并入库。支持「重新生成」清空回到表单。
- **保存与历史**：结果页提供「保存到我的 Listing」按钮，将当前标题/关键词/五点/描述及可选 analyzeResult、aplusCopy 写入表 `amazon_listing_snapshots`；保存后显示「查看历史 →」链接。工作台侧栏新增「Listing 历史」入口（`/dashboard/listings`），列表展示已保存记录，点击可查看详情并再次复制。
- **导出 CSV**：结果页与 Listing 历史详情页均提供「导出 CSV」按钮。CSV 使用亚马逊通用列头：`item_name`, `bullet_point_1`～`bullet_point_5`, `product_description`, `generic_keyword`，UTF-8 带 BOM，用户可将对应列复制到自己的批量上传文件中。实现见 `src/lib/exportAmazonListingCsv.js`。
- **生图准则与提示词**：Step 3 产品图（1～9 张可选）、Step 4 A+ 图片的完整 prompt 与准则见 `docs/AMAZON-LISTING-IMAGE-PROMPTS.md`。前端生图前展示「生成数量」与「预计消耗 X 积分」、生图时展示「正在生成产品图（共 N 张）…」；后端先校验总积分再按张顺序生成，日志按「正在生成第 n/N 张」输出。

### 9.2 后端接口

- **鉴权**：`/api/ai-assistant/amazon/*` 均使用 `requireAuth`，请求头 `Authorization: Bearer <token>`。
- **Step 1 — `POST /api/ai-assistant/amazon/analyze`**
  - 请求体：`images`（字符串数组，每项为 data URL，至少 1 个）、`category1`、`category2`、`brand`、`sellingPoints`（字符串数组，至少 2 条）、`market`、`lang`；可选 `keywords`、`notes`。
  - 响应：`{ productName, productSummary, keyAttributes, suggestedCategory }`。使用首张图 + 表单内容调用 Gemini 多模态分析，不扣积分。
- **Step 2 — `POST /api/ai-assistant/amazon/generate-listing`**
  - 请求体：`analyzeResult`（Step 1 的完整响应）、以及同上表单字段（category1、category2、brand、sellingPoints、market、lang、keywords、notes）。
  - 响应：`{ title, searchTerms, bullets, description }`。纯文本 Gemini 调用，不扣积分。后端对输出做合规截断：标题 ≤200 字符、五点每条 ≤500 字符、描述 ≤2000 字符、searchTerms ≤250 bytes（按 UTF-8 字节）。
- **Step 3 — `POST /api/ai-assistant/amazon/generate-product-images`**（requireAuth，扣积分）
  - 请求体：`productImage`（data URL，步骤 1 首图）、`productName`、`brand`；可选 `model`（默认 Nano Banana 2）、`count`（1～9，默认 1）。客户可选生成张数：1 = 仅主图，2～9 = 主图 + 附加图。
  - 响应：`{ mainImage, additionalImages[], pointsUsed, newBalance }`。第 1 张为主图（纯白底、产品约 85%、无文字），第 2～N 张为附加图（多角度或简单场景），顺序生成，每张存图库，按张扣积分；先校验总积分再调 API。
- **Step 4 A+**：复用现有 `POST /api/amazon-aplus/analyze`（文案，不扣积分）、`POST /api/amazon-aplus/generate`（4 张 A+ 图，扣积分）。前端从 Listing 结果页传入 brand、product、features（卖点≥3）、copy、productImage、style、language 等。
- **积分**：方案 C；Step 1/2 及 A+ 文案不扣积分；Step 3 主图、Step 4 A+ 图片按张扣积分。
- **存储（方案一）**：表 `amazon_listing_snapshots`（id, user_email, created_at, name, title, search_terms, bullets, description, analyze_result, aplus_copy, main_image_id, aplus_image_ids）。`POST /api/ai-assistant/amazon/save-listing` 保存当前结果；`GET /api/ai-assistant/amazon/listings` 列表；`GET /api/ai-assistant/amazon/listings/:id` 详情。图片仍存图库，表内可存 main_image_id、aplus_image_ids 引用（当前保存时未回传图库 id，可后续补齐）。

### 9.3 合规与 prompt

- `analyze`：根据产品图与表单输出结构化 productSummary，供下一步使用。
- `generate-listing`：prompt 已按第七章 7.1–7.3、7.4 全面约束：**标题**（≤200、前 80 字核心词、同词不超 2 次、禁止 `! $ ? _ { } ^ ¬ ¦`、禁止全大写/竞品/堆砌）；**五点**（5 条、每条 ≤500、建议 ≤255 以兼容部分类目、禁止价格/促销/站外/特殊符号/emoji/跨条重复/N/A/TBD/联系/质保/类目禁用词）；**描述**（≤2000、禁止与五点重复、禁止竞品/best seller 类宣称）；**searchTerms**（≤250 bytes、不重复标题与五点、逗号或空格分隔）。输出 JSON 后后端：标题去除上述禁用符号并截断至 200 字符；五点每条截断至 500；描述截断至 2000；searchTerms 按 UTF-8 字节截断至 250。
- **A+ 文案**（`/api/amazon-aplus/analyze`）：prompt 已加入 7.7 合规：禁止竞品、best seller/top-rated/Amazon's Choice、质保表述、外链/社媒/二维码、联系方式或配送细节、医疗或未证宣称、特殊符号与 emoji；要求利益导向、仅目标语言。
